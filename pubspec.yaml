name: loading_elevated_button
description: An ElevatedButton with an "isLoading" option to change Child Widget and/or disable the button when isLoading is enabled.
version: 1.0.0+1
repository: https://github.com/Eliathoran/loading_elevated_button

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true